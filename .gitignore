target/
Cargo.lock.bak
.stellar/
sdk/node_modules/
sdk/dist/
.env
.env.local
*.swp
.DS_Store
# scripts/deploy.json is committed as the canonical deploy artifact

# ZK build artifacts (circuits / ceremony / witnesses / proofs) — large, regenerable
# via zk-prover/por/build.sh and zk-prover/agg/build-jurisdictions.sh
zk-prover/por/build/
zk-prover/agg/build/
zk-prover/membership/build/

# External upstream worktrees / vendors — keep references in docs, not vendored blobs in this repo
_external/

node_modules/

# EVM (Foundry) — deps + build output; regenerate via scripts/evm/install-foundry.sh
# + `cd contracts-evm && forge install foundry-rs/forge-std`
contracts-evm/lib/
contracts-evm/out/
contracts-evm/cache/
contracts-evm/broadcast/

# venv local do Pulso — 73MB / 2.895 arquivos, artefato de máquina (add 2026-08-04)
.venv*/
venv/
__pycache__/
*.pyc
