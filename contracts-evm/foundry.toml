[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200

# DPO2U cross-chain BN254 demo (#6). The Verifier.sol here is generated by
# `snarkjs zkey export solidityverifier` from the SAME por_final.zkey that
# produced the proof verified on Stellar Soroban (por-verifier CBM6…MVCAC).
# This is the EVM half of "two chains, one proof".
