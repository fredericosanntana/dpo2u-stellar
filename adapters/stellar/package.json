{
  "name": "@dpo2u/adapter-stellar",
  "version": "0.1.0",
  "description": "Stellar/Soroban adapter for @dpo2u/core. Implements the ChainAdapter contract against the anticorruption-attestation contract, declaring exactly which attestation semantics that contract can preserve.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "4.1.6"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0",
  "dependencies": {
    "@dpo2u/core": "file:../../core"
  }
}
