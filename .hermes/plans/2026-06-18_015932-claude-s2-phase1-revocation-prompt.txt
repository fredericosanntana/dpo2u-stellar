Você está executando o **Sprint 2 / Fase 1 — Revogação protocolar real** no repositório `/root/dpo2u-stellar`.

Plano fonte obrigatório:
- `/root/dpo2u-stellar/.hermes/plans/2026-06-18_014522-sprint-2-revocation-trust-proof.md`

Contexto já implementado e verde:
- `contracts/protocol-registry`
- `contracts/asp-mvp`
- `contracts/pool-adapter-mock`
- docs do MVP em `docs/asp-protocol-mvp.md`

Escopo DESTE RUN (somente Fase 1):
1. Implementar revogação canônica no `contracts/protocol-registry`.
2. Tornar membership do `contracts/asp-mvp` mutável/inativável.
3. Fazer o `contracts/pool-adapter-mock` propagar o bloqueio quando a membership for removida/revogada.
4. Atualizar `docs/asp-protocol-mvp.md` de forma honesta para refletir a Fase 1.

Requisitos funcionais mínimos:
- `protocol-registry`
  - adicionar revogação explícita de attestation canônica
  - `verify_attestation_proof(...)` deve retornar `false` para claim revogada
  - expor getter honesto tipo `is_attestation_active(...)` ou equivalente
- `asp-mvp`
  - parar de tratar membership como permanente
  - adicionar remoção/inativação explícita de membro
  - `contains(...)` deve refletir apenas membership ativa
  - `current_root()` deve representar somente membros ativos
  - você PODE abandonar a hash chain append-only se precisar; mantenha o modelo simples e honesto
  - NÃO chame de Merkle tree nesta fase, a menos que implemente árvore real
- `pool-adapter-mock`
  - membership ativa libera
  - membership removida/revogada volta a bloquear
  - testes devem provar o caminho admitiu -> executável -> revogou/removeu -> bloqueado

Critérios de honestidade:
- não fingir privacy pool
- não fingir nullifier real
- não fingir ZK membership proof
- não implementar trust model completo de issuer ainda

Fora de escopo absoluto neste run:
- Fase 2 (trust model de issuer)
- Fase 3 (Merkle / membership proof)
- refactors não relacionados
- docs amplas fora de `docs/asp-protocol-mvp.md`, salvo pequeno ajuste estritamente necessário para consistência de build/test

Instruções de execução:
1. Leia primeiro o plano Sprint 2 e os contratos atuais.
2. Implemente apenas a Fase 1 com testes reais em Rust.
3. Mantenha o design simples, fail-closed e claramente MVP.
4. Rode validação real ao final.

Validação obrigatória:
- `cargo test -p protocol-registry`
- `cargo test -p asp-mvp`
- `cargo test -p pool-adapter-mock`
- `cargo test -p protocol-registry -p asp-mvp -p pool-adapter-mock`

Relatório obrigatório antes de parar:
- Escreva um relatório humano em `/root/dpo2u-stellar/.hermes/plans/2026-06-18_015932-claude-s2-phase1-revocation-report.md`
- O relatório deve incluir:
  - arquivos criados/modificados
  - decisões de design tomadas
  - comandos executados
  - resultado real dos testes
  - blockers/limitações honestas

Formato de saída deste processo:
- Responda em JSON (`--output-format json`) com resumo curto do que fez.
- Não faça commit, não faça push.