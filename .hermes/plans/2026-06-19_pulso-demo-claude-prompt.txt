Task: build a standalone interactive HTML demo for the Pulso submission, derived from the existing deck visual system but turned into a product/demo artifact.

Workspace: /root/dpo2u-stellar

Source files you must inspect and use as factual grounding:
- docs/submissions/PULSO-PITCH-DECK.html
- docs/S4-LIVE-REGISTRY-TO-SPP-REPORT.md
- docs/S8-EXTERNAL-ASP-BOUNDARY-REPORT.md
- integration/spp-adapter/examples/live-registry-decision.json
- integration/spp-adapter/examples/live-registry-admission.json
- integration/spp-adapter/examples/live-registry-executed.prepared.json
- integration/spp-adapter/examples/live-registry-executed.record.json
- integration/spp-adapter/examples/live-registry-decision.revoked.json
- integration/spp-adapter/examples/live-registry-s6-blocked-lane.prepared.json
- integration/spp-adapter/examples/live-registry-s6-blocked-lane.record.json

Deliverable:
- Create ONE new self-contained file:
  - docs/submissions/PULSO-DEMO.html

Goal:
- Reuse the design language of PULSO-PITCH-DECK.html, but make a demo/prototype instead of a slide deck.
- The artifact should help a judge understand the live flow:
  1. registry decision is valid
  2. admission payload is generated
  3. membership insertion executes
  4. revocation flips the registry decision
  5. blocked-lane action is prepared/executed on DPO2U-controlled lane
  6. external audited lane remains readable but not writable without admin key

Required product behavior:
- standalone HTML/CSS/JS only, no build step, no remote dependencies
- keep dark visual style coherent with the existing deck
- keyboard usable and clickable
- no filler copy, no invented claims
- include an explicit “honest boundary” panel
- include a visible state machine / stepper / timeline that the user can click through
- embed real values from the example JSON/report files (contract IDs, tx hashes, roots, request_id, etc.)
- show compact JSON/code panels or data cards for the real artifacts
- include a summary distinction between:
  - what is proven now
  - what runs on DPO2U-controlled infrastructure
  - what cannot be mutated on the external audited instance
- include a small “judge mode” or “autoplay demo flow” control if simple to implement without bloat
- persist current selected step in localStorage

Content discipline:
- Do not overclaim production pool readiness.
- Do not claim permissionless mutation of the external audited instance.
- Make it visually strong but truthful.
- Prefer a real interactive walkthrough over a marketing landing page.

Suggested structure (can improve if better):
- hero with one-liner + proof chips
- architecture rail: registry / ASP membership / blocked lane / external audited boundary
- interactive walkthrough section with 5–6 steps
- evidence section with tx hashes / roots / contract ids
- honest boundary section
- closing CTA for submission/demo recording

Verification requirements:
- after writing the file, sanity-check for obvious HTML/JS issues
- do not modify unrelated files

Output:
- Write the file only. Do not commit. At the end, print a short summary of what was created and any caveats.
