Você está executando a Fase B do sprint protocolar DPO2U dentro do repositório /root/dpo2u-stellar.

Plano fonte obrigatório:
- /root/dpo2u-stellar/.hermes/plans/2026-06-17_123119-protocol-registry-asp-pool-gating.md

Contexto já implementado e disponível:
- `contracts/protocol-registry` existe e está verde.
- Baseline congelado em `docs/asp-protocol-mvp.md`.

Escopo DESTE RUN (somente Fase B):
- Criar o novo contrato Soroban `contracts/asp-mvp`.
- Implementar testes e código mínimo para:
  - constructor(admin, registry)
  - `add_to_set(...)` gated pelo `protocol-registry`
  - `contains(...)`
  - `current_root()`
  - `leaf_count()`
- O `add_to_set(...)` deve chamar o `protocol-registry` por cross-contract call e falhar fechado quando a verificação retornar false.
- O root deve ser tratado honestamente como **deterministic append-only root MVP**, não Merkle tree completa.
- Atualizar `docs/asp-protocol-mvp.md` apenas no necessário para refletir que a Fase B passou a existir, sem vender revogação, pool real ou ZK membership proof.

Fora de escopo absoluto neste run:
- `contracts/pool-adapter-mock`
- revogação no set
- trust model completo de issuer
- docs amplas fora do baseline
- refactors não relacionados

Instruções de execução:
1. Leia primeiro o plano, `contracts/protocol-registry`, e o padrão de cross-contract call em `contracts/xchain-attest`.
2. Implemente apenas a Fase B com testes reais em Rust.
3. Mantenha o design simples e claramente MVP.
4. Rode validação real ao final.

Validação obrigatória:
- cargo test -p asp-mvp

Relatório obrigatório antes de parar:
- Escreva um relatório humano em /root/dpo2u-stellar/.hermes/plans/2026-06-17_123542-claude-phase-b-asp-mvp-report.md
- O relatório deve incluir:
  - arquivos criados/modificados
  - decisões de design tomadas
  - comandos executados
  - resultado real dos testes
  - qualquer blocker ou limitação honesta

Formato de saída deste processo:
- Responda em JSON (`--output-format json`) com resumo curto do que fez.
- Não faça commit, não faça push.